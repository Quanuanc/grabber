spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grabber?useUnicode=yes&characterEncoding=UTF-8
    username: grabber
    password: grabber
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update